# Copy this file to keystore.properties and replace values with your local secrets.
# Do NOT commit keystore.properties to source control.

storeFile=/absolute/path/to/your-release-keystore.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=REPLACE_WITH_KEY_ALIAS
keyPassword=REPLACE_WITH_KEY_PASSWORD

