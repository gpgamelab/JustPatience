<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- FIX 5: Use dark_surface_color for the stack background -->
    <solid android:color="@color/dark_surface_color" />

    <!-- A light border to make the placeholder stand out against the background_color -->
    <stroke
        android:width="1dp"
        android:color="@color/text_color_secondary" />

    <!-- Use the dimension for card corner radius -->
    <corners android:radius="@dimen/card_corner_radius" />
</shape>