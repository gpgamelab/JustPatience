<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUIRED PERMISSION FOR CONNECTING TO FASRAPI BACKEND -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Recommended for network debugging -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustPatience"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- NOTE: usesCleartextTraffic="true" is CRUCIAL: It allows local HTTP connection to 10.0.2.2 -->

        <!-- Google Play Services Metadata for AdMob -->
        <!-- Using Google's test application ID for development -->
        <!-- Replace with your real AdMob App ID when ready for production -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7092037186763886~5209725222" />

        <!-- 1. Login Activity -->
        <activity
            android:name=".LoginActivity" />

        <!-- 2. Home Activity: Landing/Menu screen (new home page) -->
        <activity
            android:name=".ui.HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JustPatience">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 3. Settings Activity -->
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!-- 3b. About Activity -->
        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:label="@string/about_title" />

        <!-- 4. Game Activity: Main gameplay screen -->
        <activity
            android:name=".ui.GameActivity"
            android:exported="true"
            android:label="@string/game_title" />

    </application>

</manifest>