<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUIRED PERMISSION FOR CONNECTING TO FASRAPI BACKEND -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Recommended for network debugging -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustPatience"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- NOTE: usesCleartextTraffic="true" is CRUCIAL: It allows local HTTP connection to 10.0.2.2 -->

        <!-- 1. Login Activity: Main Launcher -->
        <activity
            android:name=".LoginActivity" />
<!--        <activity-->
<!--            android:name=".LoginActivity"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <!-- 2. Home Activity: Primary screen after login -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.JustPatience.NoActionBar" />

        <!-- 3. Settings Activity -->
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!-- 4. Game Activity: NEW! The main gameplay screen -->
<!--        <activity-->
<!--            android:name=".ui.GameActivity"-->
<!--            android:exported="false"-->
<!--            android:screenOrientation="landscape"-->
<!--            android:label="@string/game_title"-->
<!--            android:configChanges="orientation|keyboardHidden|screenSize"/>-->
        <activity
            android:name=".ui.GameActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:label="@string/game_title"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>